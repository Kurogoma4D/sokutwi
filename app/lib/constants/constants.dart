const twitterRedirectUri = 'sokutwi://link.app';
const appCustomUrlScheme = 'sokutwi';

const fixedPhraseAnimationDuration = Duration(milliseconds: 450);

const showAd = bool.fromEnvironment('ad', defaultValue: true);
